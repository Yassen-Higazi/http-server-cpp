#include <string>
#include <vector>

using namespace std;

vector<string> split(string s, string delim);

string bufferToString(char *buffer, int buff_len);

void ltrim(string &s);

void rtrim(string &s);